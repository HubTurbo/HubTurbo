package filter.lexer;

public enum TokenType {
    EOF,
    LBRACKET,
    PERCENT,
    RBRACKET,
    SYMBOL,
    NOT,
    AND,
    OR,
    SEMICOLON,
    COLON,
    COMMA,
    QUOTE,
    DOTDOT,
    LT,
    LTE,
    GT,
    GTE,
    QUALIFIER,
    STAR,
    DATE
}
